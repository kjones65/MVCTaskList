import TaskList from './TaskList.js';

default export class TaskListController {
	constructor(taskList){
		this.taskList = taskList;
	}

	function addTask(task){
		// tell model  to add task
		// tell view to render 
	}
}

